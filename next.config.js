/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/gamelab-catalog',
  images: { unoptimized: true }
}

module.exports = nextConfig
